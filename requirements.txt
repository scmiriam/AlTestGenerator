# ── LangChain core ────────────────────────────────────────────────────
langchain
langchain-community
langchain-core
langchain-huggingface

# ── LLM providers (instala al menos uno) ─────────────────────────────
langchain-anthropic
langchain-openai
langchain-ollama

# ── Vector store & embeddings ─────────────────────────────────────────
chromadb
sentence-transformers
transformers>=4.36,<5.0

# ── Retrieval híbrido ─────────────────────────────────────────────────
rank-bm25

# ── Utilidades ML ────────────────────────────────────────────────────
tqdm
numpy
matplotlib
python-dotenv
PyYAML

# ── Procesamiento PDF / OCR ───────────────────────────────────────────
PyMuPDF
pytesseract
pdf2image
Pillow

# ── VS Code Extension API server ─────────────────────────────────────
fastapi
uvicorn[standard]

# ── Jupyter ───────────────────────────────────────────────────────────
jupyter
notebook
ipykernel

# ── Dependencia del sistema (sólo Linux/macOS) ────────────────────────
# sudo apt-get install -y poppler-utils tesseract-ocr
# En Windows: instalar Poppler manualmente y añadir al PATH
